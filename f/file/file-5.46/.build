#!/bin/bash
set -e

# File 5.46 build script based on LFS 12.4

# Configure
./configure --prefix=/usr

# Build
make

# Install
if [ -n "$DESTDIR" ]; then
    make DESTDIR="$DESTDIR" install
else
    make install
fi
